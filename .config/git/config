[user]
	email = zolisa.bleki@gmail.com
	name = "Zolisa Bleki"
    signingkey = 5BE745CFF85A3EB9

[commit]
    gpgsign = true

[alias]
    c = "checkout"
	cm = "commit -m"
	st = "status -s -b"
    nb = "checkout -b"

[core]
    editor = vim

[url "ssh://git@github.com"]
    insteadOf = "https://github.com"

[credential]
    helper = "cache --timeout=7200"

[color]
    ui = auto
    diff = auto
    status = auto
    branch = auto
    interactive = auto
    pager = true

[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
